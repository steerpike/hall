/*
const S = Math.sin, C = Math.cos;
let T = Math.tan;
const R = (r, g, b, a) => `rgba(${r}, ${g}, ${b}, ${a})`;
const c = document.querySelector("canvas");
const x = c.getContext("2d");
c.width  = window.innerWidth;
const total = c.width / 60;
console.log(total)

var t = 0;
function tick() {
	t += 1/40;
  u(t);
	requestAnimationFrame(tick);
}
requestAnimationFrame(tick);

function u(t) {
for(i=total;q=(8+t>>3)*i--;)
t%24<1?
  c.width|=r=n=>S(q**n)*9|0:
  T=f=>5*r(4)*f(r(3)*t)+9*f(r(2)*t)+60,
x.fillRect(i%total*120+T(S),(i>>4)*120+T(C),2,2)

}
*/